<% if params[:confirmed] %>
Thanks, you're subscribed.
<% end %>
<h1>AI NewsBot</h1>
<div class="row">
  <div class="col-md-9">
  <% @categories.each do |cat| %>
    <div class="card">
      <div class="card-body">
        <h4><%= cat.title %></h4>
        <table class="table">
          <tbody>
            <% cat.latest_items.each do |item| %>
              <tr>
                <td>
                  <p class = 'mb-0 pb-0'><%= link_to item.title, item.link, target: '_blank' %>
                  <small class='text-muted'>(<%= link_to item.host, item.host, class: 'text-muted' %>)</small></p>
                  <small><%= truncate(item.description, length: 100) %></small>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% end %>
  </div>
  <div class="col-md-3 ml-auto">
    <div class="card">
      <div class="card-body">
        <h4>Subcribe to the AI NewsBot</h4>
        <%= render "subscribers/form" %>
      </div>
    </div>
    <div class="card bg-success" id="thanks" style="display:none;">
      <div class="card-body">
        <p>Thanks! Your AI news is on its way!</p>
      </div>
    </div>
  </div>
</div>
